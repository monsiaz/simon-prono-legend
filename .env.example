# Compte boss — valeurs réelles dans .env.local (jamais commité) et sur Vercel.
BOSS_EMAIL=
# Hash bcrypt du mot de passe (générer : node -e "console.log(require('bcryptjs').hashSync(process.argv[1], 12))" "motdepasse")
BOSS_PASSWORD_HASH_B64=
# Secret HMAC des sessions (générer : openssl rand -hex 32)
SESSION_SECRET=
